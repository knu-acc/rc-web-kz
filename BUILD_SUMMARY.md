# Сводка подготовки к финальной сборке

## ✅ Выполненные задачи

### 1. Настройка статического экспорта
- ✅ Добавлен `output: 'export'` в `next.config.js`
- ✅ Отключена оптимизация изображений (`unoptimized: true`) для статического экспорта
- ✅ Закомментированы функции, не работающие со статическим экспортом (headers, redirects)

### 2. SEO оптимизация
- ✅ Проверены все metadata на страницах
- ✅ Sitemap.xml настроен (`app/sitemap.ts`)
- ✅ Robots.txt настроен (`app/robots.ts`)
- ✅ Schema.org разметка присутствует на всех страницах
- ✅ Open Graph и Twitter Cards настроены
- ✅ Canonical URLs настроены

### 3. Удаление лишних файлов
- ✅ Удален `brif.doc`
- ✅ Удален `public/img/reviews/README.md`
- ✅ Проверены временные файлы

### 4. Конфигурация для хостинга
- ✅ Создан `public/.htaccess` с настройками Apache
- ✅ Добавлены редиректы в `.htaccess`
- ✅ Настроено кэширование статических файлов
- ✅ Настроены заголовки безопасности

### 5. Документация
- ✅ Создан `DEPLOY.md` с инструкциями по деплою
- ✅ Создан `BUILD_CHECKLIST.md` с чек-листом перед сборкой
- ✅ Обновлен `README.md`

## 📋 Что нужно сделать перед сборкой

1. **Заменить изображения** (как вы указали)
   - Загрузите все изображения в `public/img/`
   - Проверьте пути к изображениям в коде

2. **Проверить конфигурацию**
   - Откройте `lib/constants.ts`
   - Убедитесь, что URL сайта правильный: `https://rc-web.kz`
   - Проверьте контакты (телефон, email)

3. **Выполнить сборку**
   ```bash
   npm run build
   ```

4. **После сборки**
   - Скопируйте `public/.htaccess` в `out/.htaccess`
   - Проверьте содержимое папки `out/`
   - Убедитесь, что есть `sitemap.xml` и `robots.txt`

5. **Загрузка на хостинг**
   - Загрузите ВСЁ содержимое папки `out/` в корень сайта на хостинге
   - Убедитесь, что `.htaccess` загружен и работает

## ⚠️ Важные замечания

### Статический экспорт
- API routes не работают (но у вас их нет)
- Server Actions не работают (но у вас их нет)
- Формы используют WhatsApp, что отлично для статического сайта
- Редиректы настроены в `.htaccess`, а не в `next.config.js`

### Изображения
- Все изображения должны быть в `public/img/` перед сборкой
- После сборки они будут в `out/img/`
- Убедитесь, что все пути к изображениям правильные

### Редиректы
- Редиректы настроены в `public/.htaccess`
- После сборки скопируйте `.htaccess` в `out/`
- На хостинге файл должен быть в корне сайта

## 🔍 Проверка после сборки

После выполнения `npm run build` проверьте:

1. Папка `out/` создана
2. В `out/` есть `index.html`
3. В `out/` есть `sitemap.xml`
4. В `out/` есть `robots.txt`
5. В `out/` есть папка `img/` с изображениями
6. В `out/` есть папка `_next/` со статическими ресурсами

## 📝 Команды для сборки

```bash
# Очистка (опционально)
npm run clean

# Установка зависимостей (если нужно)
npm install

# Сборка
npm run build

# Проверка линтера (опционально)
npm run lint
```

## 🚀 Готово к сборке!

После замены изображений выполните `npm run build` и следуйте инструкциям в `DEPLOY.md` для загрузки на хостинг.
