# 📚 Инструкция по загрузке проекта на GitHub

## Шаг 1: Создание репозитория на GitHub

1. Перейдите на [github.com](https://github.com) и войдите в свой аккаунт
2. Нажмите кнопку **"+"** в правом верхнем углу → **"New repository"**
3. Заполните форму:
   - **Repository name**: `rc-web-kz` (или любое другое имя)
   - **Description**: "Современный сайт разработки веб-сайтов в Алматы на Next.js"
   - **Visibility**: выберите **Public** (если хотите открытый доступ) или **Private** (если только для вас)
   - **НЕ** ставьте галочки на "Add a README file", "Add .gitignore", "Choose a license" (у нас уже есть эти файлы)
4. Нажмите **"Create repository"**

## Шаг 2: Инициализация Git в проекте

Откройте терминал (PowerShell или Command Prompt) в папке проекта и выполните:

```bash
# Перейдите в папку проекта
cd "C:\Users\User\Desktop\сайт"

# Инициализируйте Git репозиторий
git init

# Добавьте все файлы в staging
git add .

# Создайте первый коммит
git commit -m "Initial commit: Next.js website with 15 SEO-optimized pages"
```

## Шаг 3: Подключение к GitHub

```bash
# Добавьте удаленный репозиторий (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/rc-web-kz.git

# Переименуйте основную ветку в main (если нужно)
git branch -M main

# Отправьте код на GitHub
git push -u origin main
```

**Важно**: Замените `YOUR_USERNAME` на ваш реальный GitHub username!

## Шаг 4: Проверка

После выполнения команд:
1. Обновите страницу репозитория на GitHub
2. Вы должны увидеть все файлы проекта

## 🔄 Как скачать и запустить проект с GitHub

### Вариант 1: Клонирование (рекомендуется)

```bash
# Клонируйте репозиторий
git clone https://github.com/YOUR_USERNAME/rc-web-kz.git

# Перейдите в папку проекта
cd rc-web-kz

# Установите зависимости
npm install

# Запустите проект
npm run dev
```

### Вариант 2: Скачать ZIP

1. На странице репозитория нажмите зеленую кнопку **"Code"**
2. Выберите **"Download ZIP"**
3. Распакуйте архив
4. Откройте терминал в папке проекта
5. Выполните:
   ```bash
   npm install
   npm run dev
   ```

## 📤 Как обновить проект на GitHub

После внесения изменений в проект:

```bash
# Проверьте статус изменений
git status

# Добавьте измененные файлы
git add .

# Создайте коммит с описанием изменений
git commit -m "Описание ваших изменений"

# Отправьте изменения на GitHub
git push
```

## 🔐 Если GitHub запрашивает авторизацию

Если при `git push` запрашивается логин/пароль:

1. **Используйте Personal Access Token** вместо пароля:
   - Перейдите: GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
   - Нажмите "Generate new token"
   - Выберите права: `repo` (полный доступ к репозиториям)
   - Скопируйте токен и используйте его как пароль

2. **Или используйте GitHub Desktop** (графический интерфейс)

## 📋 Полезные команды Git

```bash
# Проверить статус
git status

# Посмотреть историю коммитов
git log

# Отменить изменения в файле
git checkout -- filename

# Посмотреть удаленные репозитории
git remote -v
```

## ✅ Готово!

Теперь ваш проект на GitHub, и вы можете:
- Скачивать его с любого компьютера
- Показывать другим разработчикам
- Деплоить на Vercel/Netlify напрямую из GitHub
- Хранить историю изменений
